{
  "name": "@amplication/server",
  "version": "1.1.5",
  "private": false,
  "scripts": {
    "serve": "ts-node-dev --inspect=:59229 --rs --respawn --exit-child --tree-kill -r tsconfig-paths/register --project tsconfig.app.json src/main.ts",
    "kill": "fkill :53000 -f -s"
  },
  "dependencies": {
    "@nestjs/apollo": "^10.2.0",
    "apollo-server-core": "^3.11.1"
  },
  "devDependencies": {
    "@types/node": "^18.13.0"
  }
}
